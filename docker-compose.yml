networks:
  transcendance:
    driver: bridge

services:

  nginx:
    container_name: nginx
    build: ./nginx
    ports:
      - 80:80  # Forward HTTP traffic on port 80
      - 443:443
    depends_on:
      - backend
      - frontend
    networks:
      - transcendance

  frontend:
    container_name: frontend
    build: ./front-react
    ports:
      - "3000"
    volumes:
      - frontend:/app
    depends_on:
      - backend
    networks:
      - transcendance
  backend:
    container_name: backend
    build: ./backend
    ports:
      - "8000"
    networks:
      - transcendance
    
volumes:
  frontend:
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
      device: '${VOLUME_PATH}'
      
    